# Phase 0A-P plan

The signed Darwinex MT5 binary tree is copied (never moved) to `C:\\Users\\Gasper\\NoraMt5Portable\\app` and started only with `/portable`. No broker settings are staged. The copy and the original data root are manifest-hashed before/after.

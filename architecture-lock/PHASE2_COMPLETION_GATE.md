# Nora AI Trading Lab — Phase-2 Completion Gate

Phase 3 may not begin until this gate is closed and a separate authorization is signed. Searchability remains false throughout gate work.

## Gate formula

```text
PHASE2_GATE := E1 AND E2 AND E3 AND E4 AND E5 AND signed(D1..D7)
PHASE3_MAY_BE_CONSIDERED := PHASE2_GATE
PHASE3_AUTHORIZED := PHASE2_GATE AND signed(D8)
SEARCHABLE(component) := parity_verified(component)
                         AND active_grammar_dependency(component)
                         AND explicit_human_promotion(component)
                         AND PHASE3_AUTHORIZED
```

## Mandatory evidence

### E1 — Corrected ten-strategy native execution

Run four fresh packages from the corrected Wilder-ATR v2 compiler packet:

- GDAXI/M1 A1;
- GDAXI/M1 A2;
- AUDCAD/M1 B1;
- AUDCAD/M1 B2.

Every package must:

- use the persistent repository-owned Windows runner;
- bind the corrected source, EX5, compiler evidence, execution contract, marker contract, budget map, and context identities;
- pass the frozen Option-B environmental contract;
- contain complete pre/post environment evidence, journals, ledgers, inventories, and hashes;
- publish atomically;
- be accepted by the genuine Linux importer.

No price acquisition, history expansion, unclassified mutation, missing evidence, or ambiguous package may pass.

### E2 — Exact trade-ledger reconciliation

For all ten strategies in all four packages:

- reconcile against sealed corrected Rust evidence;
- use the frozen field-specific budget map;
- require exact structural equality where the contract requires it;
- prove A1 equals A2 and B1 equals B2;
- preserve and diagnose any divergence;
- never widen a tolerance after seeing output.

Accept only with a formal identity-bound native acceptance record.

### E3 — Whole-experiment deterministic replay

Commit one Linux replay bundle that runs twice through:

```text
ingestion → aggregation → indicators → AST intents → simulation → metrics
```

The two runs must have semantically identical trades, metrics, outcomes, and canonical content identities.

### E4 — Placebo / scrambled-edge destruction

Commit a deterministic fixture with:

- synthetic data containing a planted edge;
- a canary that captures it;
- a predeclared scramble and seed;
- a predeclared edge statistic and destruction threshold;
- permanent CI coverage.

The scrambled data must destroy the edge below the frozen threshold.

### E5 — Machine-readable gate closure

Update the Phase-2 gate matrix so every binding row is:

- accepted with evidence identity; or
- explicitly reclassified by a signed human decision.

No silent shrinkage of scope is allowed.

## Mandatory human decisions

| ID | Decision | Recommended resolution |
|---|---|---|
| D1 | Scope of Layer-1 native parity | Require full admission only for initial grammar dependencies; defer remaining Layer-1 admission chains |
| D2 | Parity budgets | Per-fixture, versioned, empirically derived; global tolerance prohibited |
| D3 | Phase-3 execution-policy convention | Use bar-open signal/time exits for initial grammars; retain suite close-exit policy as a distinct regression identity |
| D4 | Ranking criterion 4 | Pre-register net profit or average trade separately for each family |
| D5 | Trade floors and drawdown ceilings | Pre-register by family/timeframe before results exist |
| D6 | Phase-3 metric unit and cost basis | Prefer per-unit/R-multiple screening with a versioned cost approximation; otherwise gross screening plus mandatory cost stress |
| D7 | Initial datasets, splits, and lockbox | Gasper-prepared contracts; lockbox sealed at first production ingestion; trial counts ledgered |
| D8 | Phase-3 authorization | Separate signed decision after E1–E5 and D1–D7 |

## Grammar-dependency closure proposed for D1

Initial full admission should cover only components required by the two first grammars:

- EMA;
- Wilder ATR;
- DistanceAtr;
- Slope;
- Cross;
- Highest and Lowest;
- comparisons and AND;
- entry intents;
- signal, time, and bracket exits;
- selected execution policy.

Other Layer-1 components remain implemented but non-searchable until their own admission chains are completed.

## Recommended hardening that does not itself close Phase 2

- Typed AST admission of Cross and Slope.
- Single-store registry prototype derived from sealed manifests.
- Suite-to-simulator equivalence fixture.
- Bounded control-plane worker pool.
- Tester-kill/VM-restart recovery exercise.
- Explicit disposition of historical untracked Phase-0A directories.

## Current verdict

At the current canonical evidence snapshot:

- E1 — corrected ten-strategy native execution: accepted narrowly;
- E2 — exact trade-ledger reconciliation: accepted narrowly;
- E3 — whole-experiment deterministic replay: accepted narrowly;
- E4 — placebo/scrambled-edge destruction: accepted narrowly;
- E5 — machine-readable gate closure: accepted with signed D1–D7 and evidence-bound node reconciliation;
- D1–D7 — signed by Gasper in the bound decision artifact;
- D8 — unsigned by design;
- gate: complete under the frozen Phase-2 scope;
- Phase 3 unauthorized;
- all components remain non-searchable.

Broker-native finalist edge survival remains a later Phase-7 contract under the pre-closeout roadmap and is not imported into Phase 2.

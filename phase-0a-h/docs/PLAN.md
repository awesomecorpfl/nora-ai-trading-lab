# Phase 0A-H plan

See the user-facing plan recorded before implementation. This spike uses only the terminal's `bases\\Custom` namespace and a fixed CSV generated on Fedora at runtime; it never writes `bases\\Darwinex-Live`.
